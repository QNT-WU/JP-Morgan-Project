[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "basic-model-tf"
version = "0.1.0"
description = "Basic (k,z) model with Mailer/MMW objectives implemented in TensorFlow"
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "tensorflow",
    "tensorflow-probability",
    "numpy",
    "matplotlib",
    "pytest"
]

[tool.setuptools]
packages = ["basic_mailer", "Experiment"]
